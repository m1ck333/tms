# @tms/us-pwa

US mobile PWA (Vite app). **Placeholder** — scaffolded and wired to `@tms/ui`,
`@tms/core`, `@tms/theme-us` in Phase 2.5. Shares logic with `us-dashboard`;
differs in form factor (mobile shell, touch UI, offline).
