# @tms/eu-pwa

EU mobile PWA (Vite app). **Placeholder** — scaffolded and wired to `@tms/ui`,
`@tms/core`, `@tms/theme-eu` in Phase 2.5. Shares logic with `eu-dashboard`;
differs in form factor (mobile shell, touch UI, offline).
