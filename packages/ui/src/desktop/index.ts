// Desktop — dashboard-only components that assume hover, keyboard and dense
// data on a wide screen (e.g. DataTable, sidebar nav, detail drawers,
// multi-column forms). Consumed by *-dashboard apps. May import primitives;
// must NEVER import from ../mobile.
export {}
