// Mobile — pwa-only components that assume touch, thumb-reach and a small
// screen (e.g. bottom nav, mobile app shell, touch lists/cards, install &
// offline UI). Consumed by *-pwa apps. May import primitives; must NEVER
// import from ../desktop.
export {}
