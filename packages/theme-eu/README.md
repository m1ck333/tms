# @tms/theme-eu

EU brand skin for the shared `@tms/ui` components: Tailwind 4 CSS-variable tokens
(color palette) + logo asset. **Colors and logo only — no component logic.**

Consumed by `eu-dashboard` and `eu-pwa`. Swap this for `@tms/theme-us` and the
same components render in the US brand.
