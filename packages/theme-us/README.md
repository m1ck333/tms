# @tms/theme-us

US brand skin for the shared `@tms/ui` components: Tailwind 4 CSS-variable tokens
(color palette) + logo asset. **Colors and logo only — no component logic.**

Consumed by `us-dashboard` and `us-pwa`. Swap this for `@tms/theme-eu` and the
same components render in the EU brand.
